@model IEnumerable<FluxoCaixa.Application.ViewModel.CollaboratorViewModel>
@{
    ViewData["Title"] = "Index";
}

<div>
    <h1>Lista de colaboradores</h1>
</div>
<hr />
<p>
    <a class="btn btn-primary" asp-action="Create">
        <i class="fas fa-plus me-1"></i>Adicionar novo colaborador
    </a>
</p>
<div class="panel panel-default">
    <table class="table table-light">
        <thead>
            <tr>
                <th scope="col">@Html.DisplayNameFor(model => model.FullName)</th>
                <th scope="col" class="text-end">Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr id="row-@user.Id">
                    <td class="align-middle">
                        @Html.DisplayFor(modelItem => user.FullName)
                    </td>
                    <td class="text-end">
                        <div class="btn-group" role="group">
                            <a class="btn btn-primary btn-sm d-inline-flex align-items-center me-2" asp-action="Edit" asp-route-id="@user.Id">
                                <i class="fas fa-edit me-1"></i><span>Editar</span>
                            </a>
                            <a class="btn btn-info btn-sm d-inline-flex align-items-center me-2" asp-action="Details" asp-route-id="@user.Id">
                                <i class="fas fa-info-circle me-1"></i><span>Detalhes</span>
                            </a>
                            <button onclick="confirmDelete(@user.Id)" title="Excluir" class="btn btn-danger btn-sm d-inline-flex align-items-center">
                                <i class="fas fa-trash-alt me-1"></i><span>Excluir</span>
                            </button>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<script src="~/js/Collaborator/Index.js"></script>

<style>
    .btn-group {
        display: flex;
        gap: 5px;
    }

    .btn-sm {
        padding: 0.25rem 0.5rem;
        white-space: nowrap;
    }

    .table td {
        vertical-align: middle;
    }

    /* Garantir que os ícones e texto fiquem na mesma linha */
    .btn-group .btn {
        display: inline-flex !important;
        align-items: center !important;
    }

        .btn-group .btn i {
            margin-right: 4px;
        }
</style>
