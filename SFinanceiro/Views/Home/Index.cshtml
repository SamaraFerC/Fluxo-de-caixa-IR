<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - FluxoCaixa</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/cosmo/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/SFinanceiro.styles.css" asp-append-version="true" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
            margin: 0;
            padding: 0;
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #000000; /* Cor preta para o texto */
        }

        .welcome-container {
            padding: 2rem;
            animation: fadeIn 1s ease-in;
        }

        .main-title {
            color: #000000; /* Cor preta */
            font-size: 3.5rem;
            font-weight: bold;
            margin-bottom: 1.5rem;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
        }

        .sub-title {
            color: #000000; /* Cor preta */
            font-size: 2.5rem;
            margin-bottom: 3rem;
        }

        .features-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            padding: 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 15px;
            padding: 2rem;
            text-align: center;
            transition: transform 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .feature-card:hover {
            transform: translateY(-5px);
        }

        .feature-icon {
            font-size: 2.5rem;
            color: #0097a7;
            margin-bottom: 1rem;
        }

        .feature-title {
            color: #000000; /* Cor preta */
            font-size: 1.25rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }

        .feature-description {
            color: #000000; /* Cor preta */
            font-size: 1rem;
        }

        .welcome-image {
            max-width: 100%;
            height: auto;
            margin-top: 2rem;
        }

        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .cta-button {
            display: inline-block;
            padding: 1rem 2rem;
            font-size: 1.2rem;
            color: white;
            background: linear-gradient(45deg, #006064 0%, #00838f 100%);
            border-radius: 30px;
            text-decoration: none;
            margin-top: 2rem;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 8px rgba(0, 0, 0, 0.2);
            color: white;
        }

        .stats-container {
            background: rgba(255, 255, 255, 0.9);
            padding: 2rem;
            border-radius: 15px;
            margin-top: 3rem;
            max-width: 1200px;
            margin-left: auto;
            margin-right: auto;
        }

        .stat-item {
            text-align: center;
            padding: 1rem;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: bold;
            color: #000000; /* Cor preta */
        }

        .stat-label {
            color: #000000; /* Cor preta */
            font-size: 1rem;
        }
    </style>
</head>
<body>
    <div class="welcome-container text-center">
        <h1 class="main-title">Sistema de Fluxo de Caixa</h1>
        
        <div class="features-container">
            <div class="feature-card">
                <i class="fas fa-chart-line feature-icon"></i>
                <h3 class="feature-title">Controle Financeiro</h3>
                <p class="feature-description">Gerencie suas finanças com facilidade e precisão</p>
            </div>
            <div class="feature-card">
                <i class="fas fa-users feature-icon"></i>
                <h3 class="feature-title">Gestão de Colaboradores</h3>
                <p class="feature-description">Organize sua equipe de forma eficiente</p>
            </div>
            <div class="feature-card">
                <i class="fas fa-tasks feature-icon"></i>
                <h3 class="feature-title">Atividades</h3>
                <p class="feature-description">Gerencie e controle suas atividades </p>
            </div>
        </div>
    </div>
</body>
</html>
