@* @model FluxoCaixa.Application.ViewModel.CashFlowViewModel
@{
    ViewData["Title"] = "Cadastro de Colaborador";
}
<h1 class="text-center mb-4">@ViewData["Title"]</h1>
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm p-4">
            <form asp-action="Create"> 
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                <input type="hidden" asp-for="Status" value="true" />

                <!-- Tipo de Parceiro -->
                <div class="mb-4">
                    <h4 class="text-muted"><strong>Tipo de Parceiro</strong></h4>
                    <label asp-for="CollaboratorTypeID" class="form-label">Selecione o Tipo</label>
                    @Html.DropDownListFor(model => model.CollaboratorTypeID, (SelectList)ViewBag.collaboratorTypes, "Selecione..", new { @class = "form-control" })
                    <span asp-validation-for="CollaboratorTypeID" class="text-danger"></span>
                </div>

                <!-- Nome Completo -->
                <div class="form-group mb-3">
                    <label asp-for="FullName" class="form-label">Nome Completo</label>
                    <input asp-for="FullName" class="form-control" placeholder="Insira o nome completo" />
                    <span asp-validation-for="FullName" class="text-danger"></span>
                </div>

                <div class="form-group mb-3">
                    <label asp-for="Id" class="form-label">CPF/CNPJ</label>
                    <input asp-for="Id" class="form-control" placeholder="Insira o nome CPF ou CNPJ"/>
                    <span asp-validation-for="Id" class="text-danger"></span>
                </div>

                <!-- Data de Nascimento -->
                <div class="form-group mb-3">
                    <label asp-for="DateBirth" class="form-label">Data de Nascimento</label>
                    <input asp-for="DateBirth" type="date" class="form-control" />
                    <span asp-validation-for="DateBirth" class="text-danger"></span>
                </div>

                <!-- Endereço -->
                <div class="mb-4">
                    <h4 class="text-muted"><strong>Endereço</strong></h4>
                    <div class="form-group mb-3">
                        <label asp-for="addressVM.Street" class="form-label">Rua</label>
                        <input asp-for="addressVM.Street" class="form-control" placeholder="Insira a rua" />
                    </div>                
                    <div class="form-group mb-3">
                        <label asp-for="addressVM.City" class="form-label">Cidade</label>
                        <input asp-for="addressVM.City" class="form-control" placeholder="Insira a cidade" />
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="addressVM.State" class="form-label">Estado</label>
                        <input asp-for="addressVM.State" class="form-control" placeholder="Insira o estado" />
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="addressVM.CEP" class="form-label">CEP</label>
                        <input asp-for="addressVM.CEP" class="form-control" placeholder="Insira o CEP" />
                    </div>
                </div>          

                <!-- Botões de Ação -->
                <div class="form-group text-center">
                    <input type="submit" value="Salvar" class="btn btn-primary me-2" />
                    <a class="btn btn-secondary" asp-action="Index">Voltar</a>
                </div>
            </form>
        </div>
    </div>
</div>

<style>
    h1 {
        font-weight: 700;
        color: #4a4a4a;
    }

    .card {
        border: 1px solid #ddd;
        border-radius: 8px;
        background-color: #f9f9f9;
    }

    .form-label {
        font-weight: 600;
        color: #6c757d;
    }

    .form-control {
        border-radius: 5px;
        box-shadow: none;
        border: 1px solid #ccc;
    }

    .btn-primary {
        background-color: #007bff;
        border-color: #007bff;
    }

    .btn-secondary {
        background-color: #6c757d;
        border-color: #6c757d;
    }
</style>

<script src="~/js/Collaborator/Index.js"></script>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
 *@